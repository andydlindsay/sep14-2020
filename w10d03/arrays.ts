const numbers: (boolean | string)[] = [true, false, 'bye'];

numbers.push(true);
numbers.push(true);
numbers.push(true);
numbers.push('hello');

const nestedArray: (string | Number)[][] = [];

nestedArray.push(['hello']);
// nestedArray.push(true);
nestedArray.push([7]);
